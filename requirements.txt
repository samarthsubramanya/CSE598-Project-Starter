google-genai>=0.3.0
python-dotenv>=1.0.0

# map_screenshot.py only (map_export.py's HTML animation needs neither):
requests>=2.31.0
Pillow>=10.0.0

# demo.ipynb only, to run it as a notebook (not needed for any .py script):
jupyter>=1.0.0
